{
  "name": "findash",
  "private": true,
  "scripts": {
    "api:dev": "pnpm --filter api start:dev",
    "web:dev": "pnpm --filter web start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
